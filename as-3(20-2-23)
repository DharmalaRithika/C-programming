#include<stdio.h>
int main()
{
int i,n,count=0;
printf("enter the number");
scanf("%d",&n);
if(n==1)
{
printf("not a prime number");
}
for(i=2;i<n;i++)
{
if(n%i==0)
count++;
if(count==0)
{
printf("%d is prime number",n);
}
}
