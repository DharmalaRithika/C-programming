// find the second highest number among n  numbers
#include<stdio.h>
int main()
{
int i, n, a, b, num;
a=b=0;
scanf("%d",&n);
for(i=0; i<n; i++)
{
scanf("%d", &num);
if (num > a)
{
b = a;
a = num;
}
else if (num > b)
b = num;
}
printf("\n second highest number is : %d",b);
return 0;
}
